/**
 * Menu for changing settings from the user. This means, here you can
 * change your teams and so on.
 */
export default async function Settings() {
    // fetching the details from the user
    return (
        <div>

        </div>
    )
}